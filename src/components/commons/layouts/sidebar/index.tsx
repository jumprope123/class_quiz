import styled from "@emotion/styled";

const SidebarWrapper = styled.div`
    height: 400px;
    background-color: lightblue;
`;

export default function LayoutSidebar() {
    return (
        <>
            <SidebarWrapper>this is sidebar</SidebarWrapper>
        </>
    );
}
